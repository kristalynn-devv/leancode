#!/usr/bin/env bash
# Retired: Claude Code's auto mode already blocks these commands (see ../README.md).
# Kept as a no-op so an install that still points here doesn't error; remove the hook.
cat >/dev/null
exit 0
